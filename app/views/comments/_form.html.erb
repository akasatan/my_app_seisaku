<%= form_with(model: comment, local: true) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comment_title %>
    <%= form.text_area :comment_title %>
  </div>

  <div class="field">
    <%= form.label :comment_body %>
    <%= form.text_area :comment_body %>
  </div>

  <div class="field">
    <%= form.label :comment_image_id %>
    <%= form.text_field :comment_image_id %>
  </div>

  <div class="field">
    <%= form.label :tweet_id %>
    <%= form.number_field :tweet_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
